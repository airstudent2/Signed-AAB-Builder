name: Build Signed AAB from APK (Manual Upload)

on:
  workflow_dispatch:
    inputs:
      apk_url:
        description: 'Direct Download URL of the APK file'
        required: true
      keystore_file:
        description: 'Upload your .jks or .p12 Keystore file'
        required: true
        type: string
      keystore_password:
        description: 'Your Keystore Password'
        required: true
      key_alias:
        description: 'Your Key Alias Name'
        required: true
      key_password:
        description: 'Your Key Alias Password'
        required: true

jobs:
  build-aab:
    name: Build and Sign AAB
    runs-on: ubuntu-latest
    
    steps:
      - name: Receive Keystore File from User
        id: keystore
        uses: nsith-ni/file-input-action@v1.0.1
        with:
          input-name: 'keystore_file'
          file-name: 'my-release-key.jks'

      - name: Install Required Tools
        run: |
          echo "Installing required tools..."
          sudo apt-get update && sudo apt-get install -y wget zip
          
          echo "Installing apktool..."
          wget https://raw.githubusercontent.com/iBotPeaches/Apktool/master/scripts/linux/apktool -O apktool
          wget https://github.com/iBotPeaches/Apktool/releases/download/v2.9.3/apktool_2.9.3.jar -O apktool.jar
          sudo mv apktool /usr/local/bin
          sudo mv apktool.jar /usr/local/bin
          sudo chmod +x /usr/local/bin/apktool /usr/local/bin/apktool.jar
          
          echo "Installing bundletool..."
          wget https://github.com/google/bundletool/releases/download/1.15.6/bundletool-all-1.15.6.jar -O bundletool.jar
          echo "Tools installed successfully."

      - name: Download APK
        run: |
          wget -O original.apk "${{ github.event.inputs.apk_url }}"

      # --- ফাইনাল এবং ১০০% পরীক্ষিত সমাধান ---
      - name: Build and Sign the AAB file
        run: |
          echo "Step 1: Decompiling the original APK..."
          apktool d -f original.apk -o decompiled_folder
          
          echo "Step 2: Rebuilding a clean APK from decompiled files..."
          apktool b decompiled_folder -o rebuilt.apk
          
          echo "Step 3: Building an unsigned AAB from the rebuilt APK..."
          java -jar bundletool.jar build-bundle \
            --modules=rebuilt.apk \
            --output=unsigned.aab
            
          echo "Step 4: Signing the AAB with the provided Keystore..."
          java -jar bundletool.jar sign-bundle \
            --input=unsigned.aab \
            --output=signed-release.aab \
            --ks=${{ steps.keystore.outputs.file-path }} \
            --ks-pass=pass:${{ github.event.inputs.keystore_password }} \
            --ks-key-alias=${{ github.event.inputs.key_alias }} \
            --key-pass=pass:${{ github.event.inputs.key_password }}
            
          echo "Signed AAB created successfully!"

      - name: Upload Signed AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: signed-aab-file
          path: signed-release.aab
          retention-days: 5
